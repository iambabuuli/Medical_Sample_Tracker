<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/light_blue_background</item>
        <item name="colorPrimaryDark">@color/light_blue_background</item>
        <item name="colorAccent">@color/light_blue_background</item>
    </style>

    <style name="AppToolBarStyle" parent="AppTheme">
        <item name="android:textColorPrimary">#000</item>
        <item name="android:textColorSecondary">#ffff</item>
        <item name="android:drawableTint" tools:targetApi="m">#fff</item>
    </style>

</resources>
